let edad: integer = 20;
let nombre: string = "Ana";
let activo: boolean = true;
const LIMITE: integer = 5;
let total = edad + LIMITE;
let mitad = total / 2;
if (activo) { print(nombre); } else { print("inactivo"); }
while (edad > 18) { edad = edad - 1; }
foreach (valor in [1, 2, 3]) { print(valor); }
